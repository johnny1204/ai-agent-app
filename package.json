{
    "name": "ai-agent-app",
    "private": true,
    "scripts": {
        "build": "cd bookshelf && npm install && npm run build",
        "build:bookshelf": "cd bookshelf && npm install && npm run build"
    }
}